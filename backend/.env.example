# VisionAssist AI — backend config
# NOTE: Provider API keys are BYOK — supplied by the client per request, NOT stored here.

# Comma-separated list of allowed CORS origins for the frontend.
VA_CORS_ORIGINS=http://localhost:3000

# Server bind host/port (used by run scripts, not by uvicorn --reload directly).
VA_HOST=0.0.0.0
VA_PORT=8000

# Upstream request timeout (seconds) for provider calls.
VA_PROVIDER_TIMEOUT=60

# Connect timeout (seconds). Kept short so a dead network fails fast instead of
# holding the request open for the full read budget.
VA_CONNECT_TIMEOUT=10

# Shared upstream connection pool. Keeping connections warm removes a TLS
# handshake from every request after the first — the single biggest win on
# time-to-first-token. Raise max_connections for higher concurrency.
VA_MAX_CONNECTIONS=100
VA_MAX_KEEPALIVE_CONNECTIONS=20
VA_KEEPALIVE_EXPIRY=60

# Close a WebSocket session that has been silent this long (seconds), so an
# abandoned tab doesn't pin a connection and its in-memory API key open.
VA_WS_IDLE_TIMEOUT=300

# Log verbosity and format. Use "json" in deployed environments so log
# aggregators get structured fields instead of a format they must re-parse.
VA_LOG_LEVEL=INFO
VA_LOG_FORMAT=text

# Hard ceiling on a JSON request body (bytes). Enforced before the body is
# buffered — pydantic can only reject an oversized payload after Starlette has
# already read all of it into memory.
VA_MAX_BODY_BYTES=25165824

# Largest accepted speech-to-text upload (bytes). Matches OpenAI's own 25 MB
# Whisper limit; anything larger is rejected upstream regardless.
VA_MAX_AUDIO_BYTES=26214400

# Longest text accepted for synthesis (characters). OpenAI's speech endpoint
# caps input at 4096.
VA_MAX_TTS_CHARS=4096

# --- Rate limiting -------------------------------------------------------
# Token bucket per client. Every limited endpoint fans out to a paid upstream
# API, so an unthrottled retry loop can burn the user's provider quota and
# saturate the shared connection pool.
# NOTE: in-process. Behind multiple replicas the effective limit multiplies by
# the replica count — limit at the load balancer if that matters.
VA_RATE_LIMIT_ENABLED=true
VA_RATE_LIMIT_RPS=2
VA_RATE_LIMIT_BURST=20

# Concurrent WebSocket sessions allowed from one client address.
VA_MAX_WS_SESSIONS_PER_CLIENT=8

# Trust X-Forwarded-For for client identity. Enable ONLY behind a proxy that
# overwrites the header — any client can send it, so trusting it while directly
# exposed lets a caller mint a new identity per request and bypass the limit.
VA_TRUST_PROXY_HEADERS=false

# --- On-screen automation (M4) -------------------------------------------
# OFF by default. This is the only part of the service that *acts* rather than
# answers, so it does not enable itself — an operator has to opt in.
#
# Requires the optional Playwright dependency:
#   pip install -r requirements-automation.txt && python -m playwright install chromium
VA_AUTOMATION_ENABLED=false

# Run the automation browser headless. Set false to watch a plan execute locally.
VA_AUTOMATION_HEADLESS=true
